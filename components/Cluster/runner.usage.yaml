definition: Different demos for AeronCluster
family: service
commands:
  exec_node_v0:
    help: run one of the cluster nodes
  docker_build_push:
    help: build and push service docker container
  exec_client:
    help: run client
  gradle_build:
    help: build client
