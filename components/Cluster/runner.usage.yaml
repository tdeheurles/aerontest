definition: A simple module to test aeron
family: service
commands:
  e2e:
    help: stop, clean, build and start client
    files:
      - runner.gradle_build.sh
      - runner.start.sh

  start:
    help: start client

  exec_0:
    help: run node 0 of the cluster
    files:
      - exec_common.sh

  exec_1:
    help: run node 1 of the cluster
    files:
      - exec_common.sh

  exec_2:
    help: run node 2 of the cluster
    files:
      - exec_common.sh

  docker_build_push:
    help: build and push service docker container

  exec_client:
    help: run client
    files:
      - exec_common.sh

  gradle_build:
    help: build client
