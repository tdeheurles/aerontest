definition: A simple module to test aeron
family: service
commands:
  e2e:
    help: stop, clean, build and start client
    files:
      - runner.build.sh
      - runner.start.sh
  build:
    help: build client
  start:
    help: start client
  exec_0:
    help: run all nodes of the cluster
    files:
      - runner.exec_common.sh
  exec_1:
    help: run all nodes of the cluster
    files:
      - runner.exec_common.sh
  exec_2:
    help: run all nodes of the cluster
    files:
      - runner.exec_common.sh
